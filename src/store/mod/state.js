import includedComponents from '@/router/isKeepAlive';
// import { rxLocalStorage } from '@/common/utils';

const state = {
  loading: false,
  keepAlive: includedComponents // 保持状态列表
};
export default state;
